Quick setup — if you’ve done this kind of thing before
or	
https://github.com/LuuSangCntt/SyncDataGen.git
Get started by creating a new file or uploading an existing file. We recommend every repository include a README, LICENSE, and .gitignore.

…or create a new repository on the command line
echo "# SyncDataGen" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/LuuSangCntt/SyncDataGen.git
git push -u origin main
…or push an existing repository from the command line
git remote add origin https://github.com/LuuSangCntt/SyncDataGen.git
git branch -M main
git push -u origin main



*Chú ý: trong manifest của project chính, cần thêm đủ các thư viện dependency của perception thì mới hết lỗi
  "dependencies": {
    "com.unity.burst": "1.7.3",
    "com.unity.collections": "1.2.4",
    "com.unity.nuget.newtonsoft-json": "3.0.2",
    "com.unity.render-pipelines.core": "12.1.7"
  },